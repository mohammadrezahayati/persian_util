/**
 * An interface representing a phone number list item.
 * @interface phoneNumberListType
 * @property {string} province - The province associated with the phone number.
 * @property {string} number - The phone number itself.
 */
export interface phoneNumberListType {
  province: string;
  number: string;
}
